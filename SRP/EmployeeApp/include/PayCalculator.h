class PayCalculator
{
    private:
        /* data */
    public:
        PayCalculator(/* args */);
        ~PayCalculator();

        float CalculateTotalPay();
        float CalculateRegularPay();
        float CalculateOvertimePay();
};
